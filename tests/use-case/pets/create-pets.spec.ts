import { beforeEach, it } from 'vitest'


describe('Register Pets Use Case', () => {

    beforeEach(() => {

    })

    it('should be able to register pets', async () => {
        const pets = await create({

        })
    })
})